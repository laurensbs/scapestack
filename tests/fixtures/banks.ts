// Re-export from the shared library location so the dev/layout page can use
// the same fixtures the tests do without duplicating them.
export {
  SMALL_MAIN_BANK,
  MAX_MAIN_BANK,
  SKILLER_BANK,
  IRONMAN_BANK
} from "@/lib/fixtures";
