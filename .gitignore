node_modules
.next
out
dist
.env*.local
*.log
.DS_Store
*.tsbuildinfo

# Next.js auto-generates this and switches its content between dev / build
# runs (.next/dev/types vs .next/types). Tracking it causes endless diffs.
next-env.d.ts

# Old working copies — kept locally, never published. See _archive/README.md.
_archive/

# Machine-specific Claude Code permissions. launch.json IS shared.
.claude/settings.local.json
.vercel
.artifacts/
test-results/
playwright-report/
