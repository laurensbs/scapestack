import type { Metadata } from "next";
export const metadata: Metadata = { title: "Can I leave the bank?" };
export default function BankLayout({ children }: { children: React.ReactNode }) { return children; }
